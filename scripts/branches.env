# ── Branch configuration for talentOS deploy ─────────────────────────────
# Each service repo pulls from its configured branch.
# Change these to deploy a different branch, then run deploy.sh.
# ──────────────────────────────────────────────────────────────────────────
BE_BRANCH=development
FE_BRANCH=development
MCP_BRANCH=dev
AI_BRANCH=dev
INFRA_BRANCH=main
